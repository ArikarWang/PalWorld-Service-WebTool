# Gitee 新版流水线：推送 v* tag 后自动构建并发布 Release 附件
# 失败时请打开「构建历史」把报错步骤日志发出来，便于继续改。
#
# 本机备用发版（不依赖流水线）：
#   $env:GITEE_TOKEN="..."
#   .\scripts\release-gitee.ps1 -Version v1.0.15

version: '1.0'
name: release-win-x64
displayName: 发布 win-x64

triggers:
  push:
    tags:
      prefix:
        - v

stages:
  - stage: build
    displayName: 构建打包
    strategy: naturally
    trigger: auto
    steps:
      # 社区版常见有 build@nodejs；.NET 在同一步里用官方脚本安装，避免缺 build@dotnet 插件
      - step: build@nodejs
        name: build_all
        displayName: 构建前端并打包 win-x64
        nodeVersion: 20.11.0
        commands: |
          set -eux
          echo "TAG=${GITEE_TAG:-} BRANCH=${GITEE_BRANCH:-} COMMIT=${GITEE_COMMIT:-}"

          cd web
          npm ci --registry=https://registry.npmmirror.com
          npm run build
          cd ..

          curl -fsSL https://dot.net/v1/dotnet-install.sh -o /tmp/dotnet-install.sh
          bash /tmp/dotnet-install.sh --channel 8.0 --install-dir "$HOME/.dotnet"
          export DOTNET_ROOT="$HOME/.dotnet"
          export PATH="$HOME/.dotnet:$PATH"
          dotnet --info

          VER="${GITEE_TAG#v}"
          if [ -z "$VER" ]; then VER="0.0.${GITEE_PIPELINE_BUILD_NUMBER:-0}"; fi
          echo "Embedding version: $VER"

          mkdir -p artifacts/PalWorldService/config
          dotnet publish src/PalWorldService.Host/PalWorldService.Host.csproj \
            -c Release -r win-x64 --self-contained true \
            -p:Version="$VER" -p:InformationalVersion="$VER" \
            -o artifacts/PalWorldService
          cp config/servers.yaml artifacts/PalWorldService/config/servers.yaml
          cp scripts/start.bat artifacts/PalWorldService/start.bat
          test -f artifacts/PalWorldService/PalWorldService.exe

          if command -v zip >/dev/null 2>&1; then
            (cd artifacts/PalWorldService && zip -qr ../PalWorldService-win-x64.zip .)
          else
            python3 - <<'PY'
          import shutil
          shutil.make_archive("artifacts/PalWorldService-win-x64", "zip", "artifacts/PalWorldService")
          PY
          fi

          cp artifacts/PalWorldService-win-x64.zip ./PalWorldService-win-x64.zip
          ls -lh PalWorldService-win-x64.zip
        artifacts:
          - name: PLUGIN_RELEASE
            path:
              - PalWorldService-win-x64.zip

  - stage: release
    displayName: 发版
    strategy: naturally
    trigger: auto
    steps:
      - step: release@gitee
        name: release_gitee
        displayName: Gitee Release
        releaseName: PalWorld Service ${GITEE_TAG}
        description: PalWorld Service ${GITEE_TAG}
        tagName: ${GITEE_TAG}
        upstreamArtifact:
          - PLUGIN_RELEASE
        assertFiles:
          - PalWorldService-win-x64.zip
        prerelease: false
        allowUpdate: true
